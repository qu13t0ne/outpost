#  CYBERCHEF 
version: "3"

services:
  cyberchef:
    container_name: cyberchef
    image: mpepping/cyberchef
    restart: always
    # ports:
      # - 8000:8000

networks:
  default:
    name: proxy_net
    external: true
